{{#unless noDiv}}
<div>
    {{/unless}}


    <span class="badge rounded-pill text-bg-info">
        {{name}}
    </span>

    {{#unless noLine}}
    -
    {{/unless}}

    {{data}}

    {{#unless noDiv}}
</div>
{{/unless}}