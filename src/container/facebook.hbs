<main>
    <section>

        {{>title big=true noLine=true data=name}}

        <div>
            {{>warning name='users' noLine=true sq=true}}
            {{#each users}}
            <ul>
                <li>
                    {{>warning name='user' noLine=true sq=true}}
                    {{>user}}

                    <div>
                        {{#each friends}}

                        {{>warning name='friends' noLine=true sq=true}}
                        {{>user}}

                        {{#if mutual_friends}}
                        <div>
                            <ul>
                                {{#each mutual_friends}}
                                <li>

                                    {{>warning name='mutual friend' noLine=true sq=true}}
                                    {{>user}}

                                    <div>
                                        {{>warning name='mutual friend' noLine=true sq=true}}
                                        {{#each mutual_friends}}
                                        {{>user}}
                                        {{/each}}
                                    </div>

                                </li>
                                {{/each}}
                            </ul>
                        </div>
                        {{/if}}
                        {{>groups}}
                        {{/each}}
                    </div><br>

                    {{>groups}}

                </li><br>
            </ul>
            {{/each}}
        </div>

    </section>
</main>