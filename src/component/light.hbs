{{#unless noDiv}}
<div>
    {{/unless}}


    <span class="badge {{#unless sq}}rounded-pill {{/unless}}text-bg-light">
        {{name}}
    </span>

    {{#unless noLine}}
    -
    {{/unless}}

    {{data}}

    {{#unless noDiv}}
</div>
{{/unless}}